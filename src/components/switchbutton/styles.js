import {StyleSheet} from 'react-native'
import {
    hp, wp
} from '../../theme'

export default StyleSheet.create({
    switchStyle: {
        marginTop: hp(1.2)
    },
    circleStyle: {
        width: wp(3),
        height: hp(1.5)
    }
})
